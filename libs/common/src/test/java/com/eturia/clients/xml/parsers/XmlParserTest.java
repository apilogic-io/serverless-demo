package com.eturia.clients.xml.parsers;

import com.eturia.clients.common.CommonService;
import com.eturia.clients.common.FunctionUtils;
import com.eturia.clients.common.entities.EntitiesEnum;
import com.eturia.clients.common.parsers.XmlParser;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

import java.util.Map;

public class XmlParserTest {

  @Test
  @Disabled
  public void parseHotels() throws Exception {
//    String xml = FunctionUtils.getRequestXml(this.getClass().getClassLoader().getResource("hotels.xml"));
//    CommonService.getYamlPathsConfig(EntitiesEnum.HOTELS.getValue());
//    Map<String, Object> flattenedXml = XmlParser.parse(xml, CommonService.getXmlParserConfig(EntitiesEnum.HOTELS.getValue()));
//    Assertions.assertFalse(flattenedXml.isEmpty());
  }

}
